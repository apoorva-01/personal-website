.DS_Store
node_modules/
.vercel/
.env
.env.local
.vercel

# Kokoro TTS virtualenv (torch etc.) — recreate with scripts/tts/README, never commit
scripts/tts/venv/

# Google service-account keys — NEVER commit these (they go in GitHub Actions secrets)
service_account.json
portfolio-*.json
*service-account*.json
*.iam.gserviceaccount.com.json
