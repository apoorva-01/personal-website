# Personal Website — Apoorva Verma

AI Engineer portfolio (RAG &amp; LLM Evaluation). Static site, no build step.

- **`index.html`** — portfolio: animated hero, grounded-chat demo with faithfulness scoring, RAGAS eval dashboard, projects, timeline, contact.
- **`blog.html`** — writing index with article overlays.

Dark/light theme persists via `localStorage`. Just open `index.html` in a browser, or serve the folder with any static host.
